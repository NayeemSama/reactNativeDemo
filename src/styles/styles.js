import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'space-around',
    padding: 20,
    margin: 20,
  },
  button: {
    alignItems: 'center',
    backgroundColor: '#DDd',
    padding: 10,
    borderRadius: 18,
    elevation: 2,
  },
});
